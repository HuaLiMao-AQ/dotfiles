-- ============================================================================
-- Nui.nvim UI 组件库
-- ============================================================================
-- 功能说明:
--   nui.nvim 提供 Popup / Input / Menu / Layout 等基础 UI 组件。
--   它通常作为 noice.nvim、neo-tree.nvim 等插件的底层依赖使用。
--
-- 配置效果:
--   1. 按现有插件目录格式独立拆分为单文件
--   2. 保持 lazy = true，由依赖它的插件按需加载
--   3. 方便后续统一维护 UI 基础依赖
-- ============================================================================

return {
    "MunifTanjim/nui.nvim",
    lazy = true,
}
